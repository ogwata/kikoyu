# pyannote 話者分離用の HuggingFace アクセストークン。
# 使い方:
#   1. このファイルを .env にコピー: cp .env.example .env
#   2. hf_xxx... を自分のトークンに置き換える
#   3. chmod 600 .env  (本人だけ読める権限に)
# 注意: 値はクオートで囲まないこと (docker run --env-file はクオートも値の一部として扱う)
HF_whisperx=hf_xxxxxxxxxxxxxxxxxxxxxxxx
